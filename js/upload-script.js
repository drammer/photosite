(function($){
    function generateImgInput(){
        //var linkImg = $('.preview a').attr('href');
        $('.preview a').each(function(){
            var linkImg = $(this).attr('href');

        })
    }

})(jQuery)